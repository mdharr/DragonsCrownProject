import { CalculateTotalSPPipe } from './calculate-total-s-p.pipe';

describe('CalculateTotalSPPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateTotalSPPipe();
    expect(pipe).toBeTruthy();
  });
});
